#include <stdio.h>

/**
 * main -  print  all single digit numbers of base 10 starting from 0
 * Return: Always 0 (Success)
 */

int main(void)
{
int ascii = 48;
while (ascii <= 57)
  putchar(ascii);
putchar('\n');  
return (0);
}
